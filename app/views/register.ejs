<%- include('partials/head', { title: 'Créer un compte' }) %>
<div class="min-h-screen flex items-center justify-center px-4 py-10">
  <div class="auth-grid w-full max-w-5xl">
    <div class="card glass-panel shadow-xl form-surface">
      <div class="card-body">
        <h1 class="text-2xl font-bold">Créer un compte</h1>
        <p class="text-base-content/70">Accédez au dashboard DaisyUI.</p>
        <% if (error) { %>
        <div class="alert alert-error mt-2">
          <span><%= error %></span>
        </div>
        <% } %>
        <form method="post" action="/register" class="space-y-4 mt-4">
          <label class="form-control">
            <div class="label">
              <span class="label-text">Nom</span>
            </div>
            <input type="text" name="name" class="input input-bordered" required />
          </label>
          <label class="form-control">
            <div class="label">
              <span class="label-text">Email</span>
            </div>
            <input type="email" name="email" class="input input-bordered" required />
          </label>
          <label class="form-control">
            <div class="label">
              <span class="label-text">Mot de passe</span>
            </div>
            <input type="password" name="password" class="input input-bordered" required />
          </label>
          <button class="btn btn-primary w-full" type="submit">Créer mon compte</button>
        </form>
        <p class="text-sm text-base-content/70">
          Déjà un compte ?
          <a class="link link-primary" href="/login">Se connecter</a>.
        </p>
      </div>
    </div>
    <div class="card brand-shell shadow-2xl">
      <div class="card-body">
        <span class="badge badge-outline border-white/40 text-white">Bienvenue</span>
        <h2 class="text-3xl font-bold leading-tight">Rejoignez un espace pensé pour vos idées.</h2>
        <p class="text-white/80">
          Créez vos cards, suivez l'activité et profitez d'une interface fluide pensée pour la
          collaboration.
        </p>
        <ul class="space-y-2 text-sm text-white/80">
          <li class="flex items-center gap-2">
            <span class="badge badge-xs bg-white/20 border-none"></span>
            Organisation instantanée.
          </li>
          <li class="flex items-center gap-2">
            <span class="badge badge-xs bg-white/20 border-none"></span>
            Design moderne DaisyUI.
          </li>
          <li class="flex items-center gap-2">
            <span class="badge badge-xs bg-white/20 border-none"></span>
            Hébergement simplifié avec Docker.
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
